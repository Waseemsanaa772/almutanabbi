name: Render Video
on:
  workflow_dispatch:
    inputs:
      scene:
        description: 'المشهد المراد رندرته'
        required: true
        default: 'scene1'

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744

      - name: Setup Node
        uses: actions/setup-node@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Render Scene
        run: |
          if [ "${{ github.event.inputs.scene }}" == "scene1" ]; then
            npx remotion render almutanabbi out/almutanabbi_scene1.mp4
          fi

      - name: Upload Video
        uses: actions/upload-artifact@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          name: almutanabbi-video
          path: out/almutanabbi_scene1.mp4
